{
  "name": "showCatById",
  "version": "1.0.0",
  "description": "通过Id筛选符合要求的记录，传入值说明：Id为该记录的主键，在调取相应记录时会将Id连同其他数据一起传过去，而前端需要执行该方法时，再将此参数传回；传出值说明：为一个json对象的数组和errMsg，只要errMsg为'collection.get:ok'，即说明查询过程正常，若返回值为空，说明不存在该条记录，若errMsg为错误信息，则后端查询过程存在异常",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "王晓奇",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "~2.0.3"
  }
}
